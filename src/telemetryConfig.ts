export interface TelemetryConfig {
  allowedToPassAuthenticationStatus?: boolean;
  allowedToPassRequestStatusCode?: boolean;
  allowedToPassRequestTimings?: boolean;
  allowedToPassTimeout?: boolean;
}
